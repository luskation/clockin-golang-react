# Usado só pelo docker-compose.yml da raiz para inicializar o container do Postgres.
# Deve ter os MESMOS valores de DB_USER/DB_PASSWORD/DB_NAME que estão em ponto/.env,
# já que o backend se conecta a esse mesmo banco usando aquelas credenciais.
DB_USER=
DB_PASSWORD=
DB_NAME=
